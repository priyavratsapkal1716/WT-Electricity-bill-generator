Style.css

:root {
--bg-gradient: linear-gradient(135deg, #0f172a 0%, #1e1b4b 50%,
#311042 100%);
--card-bg: rgba(255, 255, 255, 0.96);
--primary-color: #4f46e5;
--primary-hover: #4338ca;
--accent-color: #10b981;
--text-primary: #1e293b;
--text-secondary: #64748b;
--border-color: #e2e8f0;
--radius: 16px;
--shadow: 0 20px 25px -5px rgba(0, 0, 0, 0.25), 0 8px 10px -6px
rgba(0, 0, 0, 0.2);
--transition: all 0.25s cubic-bezier(0.4, 0, 0.2, 1);
}

* {
box-sizing: border-box; margin: 0;
padding: 0;
}

body {
font-family: 'Plus Jakarta Sans', system-ui, -apple-system, sans-serif;
background: var(--bg-gradient); min-height: 100vh;
display: flex;
justify-content: center; align-items: center; padding: 1.5rem;
color: var(--text-primary);
}

.container {
background: var(--card-bg); padding: 2.5rem;
border-radius: var(--radius); box-shadow: var(--shadow);
 
max-width: 440px; width: 100%;
backdrop-filter: blur(12px);
border: 1px solid rgba(255, 255, 255, 0.2); transition: var(--transition);
}

.card-header {
text-align: center; margin-bottom: 2rem;
}

.icon-badge {
display: inline-flex; align-items: center; justify-content: center; width: 52px;
height: 52px; background: #e0e7ff;
color: var(--primary-color); font-size: 1.75rem;
border-radius: 50%; margin-bottom: 0.75rem;
box-shadow: 0 4px 12px rgba(79, 70, 229, 0.15);
}

 
h1 {







}
 

font-size: 1.5rem; font-weight: 700;
color: var(--text-primary); letter-spacing: -0.02em; margin-bottom: 0.35rem;
 

.subtitle {
font-size: 0.875rem;
color: var(--text-secondary);
}

form {
 
display: flex;
flex-direction: column; gap: 1.25rem;
}

.form-group {
display: flex;
flex-direction: column; gap: 0.5rem;
}

label {
font-size: 0.875rem; font-weight: 600;
color: var(--text-primary);
}

input[type="number"] { width: 100%;
padding: 0.875rem 1rem; font-size: 1rem;
font-family: inherit; color: var(--text-primary); background-color: #f8fafc;
border: 1.5px solid var(--border-color); border-radius: 10px;
outline: none;
transition: var(--transition);
}

input[type="number"]:focus { background-color: #fff;
border-color: var(--primary-color);
box-shadow: 0 0 0 4px rgba(79, 70, 229, 0.15);
}

button {
width: 100%; padding: 0.875rem;
background: linear-gradient(135deg, #4f46e5 0%, #3730a3 100%);
 
color: #ffffff;
font-family: inherit; font-size: 1rem;
font-weight: 600; border: none;
border-radius: 10px; cursor: pointer;
box-shadow: 0 4px 12px rgba(79, 70, 229, 0.3); transition: var(--transition);
}

button:hover {
transform: translateY(-2px);
box-shadow: 0 6px 16px rgba(79, 70, 229, 0.4);
}

button:active {
transform: translateY(0);
}

button:disabled { opacity: 0.7; cursor: not-allowed; transform: none;
}

.result-box {
margin-top: 1.75rem; padding: 1.25rem; background: #f0fdf4;
border: 1.5px solid #bbf7d0; border-radius: 12px; opacity: 1;
transform: translateY(0); transition: var(--transition);
}

.result-box.hidden { display: none; opacity: 0;
 
transform: translateY(10px);
}

.result-box h2 {
font-size: 0.875rem;
text-transform: uppercase; letter-spacing: 0.05em; color: #166534;
margin-bottom: 0.5rem; font-weight: 700;
}

.total-row {
display: flex;
justify-content: space-between; align-items: center;
}

.total-row .label { font-size: 0.95rem; font-weight: 500; color: #15803d;
}

.total-row .amount { font-size: 1.35rem; font-weight: 700; color: #15803d;
}

@media (max-width: 480px) {
.container {
padding: 1.75rem;
}
}
